import sys
import time
import datetime
import csv


dateTimeNow = datetime.datetime.now().strftime("%y-%m-%d %H:%M")
dateTimeToday = datetime.date.today().strftime("%A")
print(dateTimeNow)


if dateTimeNow.strftime("%d") = "Monday"
    with open("monday.csv")

dateTimeNow.strftime("%d") = "Tuesday"

dateTimeNow.strftime("%d") = "Wednesday"

dateTimeNow.strftime("%d") = "Thursday"

dateTimeNow.strftime("%d") = "Friday"



def Monday(time):
    Block1 = 
